@namespace IMS_Interface.World

@using IMS_Library

@inject ServerProvider ServerSelector

@code {
    public Guid CurrentServerID => ServerSelector.CurrentServerID;

    [Parameter]
    public Guid CurrentWorldID { get; set; }
    public World CurrentWorld => IMS.Instance.WorldManager.GetWorldByID(CurrentWorldID);
}
