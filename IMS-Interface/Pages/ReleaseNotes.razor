@namespace IMS_Interface

@using System.Reflection
@using IMS_Library

@page "/ReleaseNotes"

<style>
    .header-label {
        font-weight: normal;
        margin-top: 5px;
    }
</style>

<h3>Release Notes</h3>
<label style="font-weight:normal"><i>IMS Prerelease v@(Assembly.GetAssembly(typeof(IMS)).GetName().Version)</i></label>
<br />
<br />
<div style="max-width:500px">
    <label class="header-label"><b>Features</b></label>
    <ul>
        <li>Updated all text to say "IntegratedMinecraftServer" without spaces in order to be constistent with the project name.</li>
        <li>Added an "experimental" subtitle to IMS that displays when IMS is running in developer mode.</li>
    </ul>
    <label class="header-label"><b>Bugfixes</b></label>
    <ul>
        <li>Added the scan/removal of firewall entries that may conflict with autogenerated IMS entries to ensure that port forwarding works.</li>
        <li>Fixed a bug where nullable types wouldn't be properly saved to configuration files.</li>
    </ul>
    <!--
    <label class="header-label"><b>Maintenance</b></label>
    <ul>
        <li>Added logging support for the IMS update routine.</li>
    </ul>-->
</div>

@code {

}
