@namespace IMS_Interface

@using System.Reflection
@using IMS_Library

@page "/ReleaseNotes"

<style>
    .header-label {
        font-weight: normal;
        margin-top: 5px;
    }
</style>

<h3>Release Notes</h3>
<label style="font-weight:normal"><i>IMS v@(Assembly.GetAssembly(typeof(IMS)).GetName().Version)</i></label>
<br />
<br />
<div style="max-width:500px">
    <!--<label class="header-label"><b>Features</b></label>
    <ul>
        <li>Added a proper warning on the worlds page when an invalid Minecraft world is uploaded.</li>
    </ul>-->
    <label class="header-label"><b>Bugfixes</b></label>
    <ul>
        <li>Editing Bedrock server oplists/whitelists now will save properly even when the server is offline.</li>
        <li>Fixed a crash that occured when attempting to run IMS in countries other than the United States. IMS will now always read/write dates to configuration files in US format, preventing their misinterpretation.</li>
        <li>Fixed a bug that prevented IMS from updating Bedrock servers to the latest version.</li>
        <li>Removed a glitch that prevented Bedrock players from being added to whitelists.</li>
    </ul>
    <label class="header-label"><b>Maintenance</b></label>
    <ul>
        <li>Imported the RoyalXml library from the Nuget package manager instead of referencing it as a DLL file, making it easier to manage as a dependency.</li>
    </ul>
</div>

@code {

}
